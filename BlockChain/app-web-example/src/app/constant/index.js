const Constant = {
    apiBase: API_URL.apiBase,
    channel: API_URL.channel,
    chaincodeName: API_URL.chaincodeName,
    chaincodePath: API_URL.chaincodePath,
    chaincodeVersion: API_URL.chaincodeVersion
};

export default Constant;
